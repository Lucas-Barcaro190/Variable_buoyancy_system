Assunto: Implementação do Controlador de Posição/Velocidade e Gerador de Trajetória Trapezoidal em FreeRTOSOlá! Precisamos implementar e refatorar a arquitetura de controle do motor no nosso projeto FreeRTOS (usando Raspberry Pi Pico / RP2040 com PIO), baseando-nos na malha de controle do arquivo blockdiagram.xml simulado no Simulink e na função do MATLAB para geração do perfil de velocidade trapezoidal.Por favor, siga rigorosamente a estrutura, diretrizes e regras descritas abaixo:1. Estrutura de Arquivos e SubpastasTodos os arquivos de controle do motor foram organizados dentro da subpasta src/motor/. Temos 6 arquivos principais + o programa PIO em assembly:src/motor/motor_control.h e src/motor/motor_control.cpp: Implementação da malha de controle PID, saturação e interface com a PIO para envio de comandos ao motor de passo.src/motor/velocity_generator.h e src/motor/velocity_generator.cpp: Implementação do perfil trapezoidal de velocidade (baseado na função MATLAB gerador_trajetoria_tempo) com zona morta de $0.15\text{ mm}$ e limites dinâmicos de rampa (aceleração entre 2s e 5s para $v_{max}$).src/motor/potentiometer_reading.h e src/motor/potentiometer_reading.cpp: Leitura e conversão da posição atual do atuador via potenciômetro (manter a lógica/calibração existente).src/motor/pwm_pio.pio: Código em assembly do PIO para geração do PWM do driver de passo.2. Mapeamento da Malha do Simulink (blockdiagram.xml) e Perfil de Vel.Referência & Perfil (velocity_generator):Entrada: Tempo $t$, $h_{start}$ e $h_{target}$.Se $\vert{}h_{target} - h_{start}\vert{} < 0.15\text{ mm}$, mantém $v_{ref} = 0$ e $h_{ref} = h_{start}$ (proteção contra ruído e zona morta do sensor).Saturação de aceleração: Tempo de rampa de aceleração entre $2\text{ s}$ e $5\text{ s}$ para $v_{max}$ ($250\text{ RPM} \approx 0.0020353\text{ mm/s}$).Seleção automática entre perfil Trapezoidal e Triangular em deslocamentos curtos.Converte a saída final de velocidade para RPM / frequência de passos.Controlador PID (motor_control):Calcula o erro de posição $e(t) = h_{ref} - h_{medido}$.Executa a malha PID em tempo discreto, aplicando a saturação de controle.Converte a saída do PID em contagens de delay/frequência para o FIFO da PIO.3. Atuação no Motor via PIO (pwm_pio.pio)Atenção: Mude a forma de atuar no motor. Anteriormente, enviava-se a quantidade total de pulsos para o PIO. Agora, a PIO atua gerando um PWM contínuo de $50\%$ de duty cycle. O valor enviado para o FIFO do PIO define o período do pulso (tempo em HIGH e tempo em LOW):Snippet de código.program stepper_variable
.wrap_target
    pull noblock    ; Puxa o novo período do FIFO. Se estiver vazio, mantém o último valor no X.
    mov x, osr      ; Copia o valor para o registrador X (nosso contador de delay)
    
    set pins, 1     ; Coloca o pino de PULSE em nível ALTO
lp1:
    jmp x-- lp1     ; Mantém em ALTO proporcionalmente ao valor em X
    
    mov x, osr      ; Recarrega o contador para o tempo em nível BAIXO
    set pins, 0     ; Coloca o pino de PULSE em nível BAIXO
lp2:
    jmp x-- lp2     ; Mantém em BAIXO proporcionalmente ao valor em X
.wrap
Ajuste o código C++ em motor_control.cpp para calcular o valor de $X$ (período) correspondente à velocidade calculada pela malha de controle e alimentá-lo no FIFO da state machine da PIO.4. Organização das Tasks FreeRTOS e Afinidade de Núcleos (Multicore)Refatoração de Tasks: Atualmente, as tasks estão agrupadas dentro de src/tasks/task_handler.cpp. Crie arquivos individuais por task dentro da subpasta tasks (ou transforme o task_handler.cpp em um conjunto de wrappers chamando as implementações separadas nos respectivos módulos em src/motor/).Afinidade de Núcleos (Core Affinity):Núcleo 0 (Core 0): As tasks dedicadas ao controle do motor — que invocam periodicamente a leitura do potenciômetro (potentiometer_reading), o gerador de trajetórias (velocity_generator) e o laço de controle PID (motor_control) — devem ter afinidade estrita com o Core 0 (use xTaskCreatePinnedToCore direcionando para o core 0).Núcleo 1 (Core 1): Todas as demais tasks do sistema (comunicação, rotinas secundárias, etc.) devem ser fixadas no Core 1.5. Regras ImportantesPreserve a lógica existente: Para a leitura e conversão do potenciômetro, baseie-se estritamente na calibração e equações já implementadas no projeto.Thread Safety: Garanta que a troca de dados de referência e medição entre as tasks utilize mecanismos nativos do FreeRTOS (como Mutex, Queues ou Direct Task Notifications), evitando condições de corrida entre os núcleos.Vamos implementar ou refatorar estes arquivos mantendo a arquitetura limpa e modular. Por favor, apresente as alterações ou arquivos sugeridos.